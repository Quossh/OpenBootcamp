// Ejercicio (Tema 5)
let altura = 185;
let altura_m = 1.85;
let peso_k = 75.40;
let max_valor_JS = Number.MAX_VALUE;

// Altura en centimetros
console.log(parseInt(altura));
// Altura en metros, coma flotante
console.log(altura_m);
// Peso en kilos, coma flotante
console.log(peso_k);
// Altura redondeada hacia arriba
console.log(Math.round(altura_m));
// Peso en kilos redondeado hacia abajo
console.log(Math.round(peso_k));

// Variable máximo valor JS
console.log(max_valor_JS);